@page "/login"
@using PuzzleGames.Frontend.Clients
@using PuzzleGames.Frontend.Logic
@using PuzzleGames.Frontend.Models
@inject NavigationManager NavigationManager
@inject UserClient UserClient
@inject UserSession UserSession

<div class="row justify-content-center mt-5">
    <div class="col-md-4">
        <EditForm Model="@user" FormName="login" OnValidSubmit="HandleSubmitAsync">
            <DataAnnotationsValidator />
            <ValidationSummary />
            <div class="mb-3">
                <label for="username" class="form-label">Username:</label>
                <InputText id="username" @bind-Value="user.Username" class="form-control"/>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password:</label>
                <InputText id="password" type="password" @bind-Value="user.Password" class="form-control"/>
            </div>
            <button type="submit" class="btn btn-primary">Login</button>
            <a role="button" class="btn btn-secondary" href="/">Cancel</a>
        </EditForm>
    </div>
</div>
@code {
    LoginUser user { get; set; } = new();

    private async Task HandleSubmitAsync()
    {
        var loggedInUser = await UserClient.GetUserByUsernameAsync(user);
        UserSession.CurrentUser = loggedInUser;
        NavigationManager.NavigateTo("/");
    }
}